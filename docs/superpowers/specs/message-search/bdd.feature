# language: zh-TW
功能: 訊息搜尋
  作為登入使用者
  我想用關鍵字跨我的所有對話搜尋訊息
  以便快速找到並跳轉到過去的訊息

  背景:
    假設 已有使用者 Alice、Bob、Carol
    並且 Alice 與 Bob 為好友且有一個 1對1 對話
    並且 Bob 與 Carol 為好友且有一個 1對1 對話（Alice 非成員）

  # ---- Happy Path ----
  場景: MS-01 以內容關鍵字命中
    假設 Bob 在與 Alice 的對話送出訊息 "明天的會議改到下午三點"
    當 Alice 以關鍵字 "會議" 搜尋
    那麼 結果包含該則訊息
    並且 該結果附帶所屬對話資訊（與 Bob 的 1對1）

  場景: MS-02 以寄件者名稱命中
    假設 Bob 在與 Alice 的對話送出訊息 "收到"
    當 Alice 以關鍵字 "Bob" 搜尋
    那麼 結果包含 Bob 寄出的該則訊息

  場景: MS-07 點搜尋結果跳轉並高亮（UI）
    假設 與 Alice 對話中存在一則較舊、不在初始可見範圍的訊息 "季報初稿"
    並且 Alice 已登入並開啟聊天畫面
    當 Alice 搜尋 "季報" 並點擊該結果
    那麼 畫面切換到與 Bob 的對話
    並且 該則訊息可見且被高亮
    並且 高亮數秒後自動消失

  # ---- Authorization ----
  場景: MS-04 不外洩他人對話
    假設 Bob 在與 Carol 的對話送出訊息 "Alice 不該看到這句"
    當 Alice 以關鍵字 "不該看到" 搜尋
    那麼 結果為空（Alice 非該對話成員）

  場景: MS-06 未授權搜尋被拒
    當 未帶 token 呼叫搜尋端點
    那麼 回應 401

  # ---- Validation Failure ----
  場景: MS-05a 空白關鍵字被拒
    當 Alice 以關鍵字 "   "（全空白）搜尋
    那麼 回應 422

  場景: MS-05b 過長關鍵字被拒
    當 Alice 以長度超過 100 字的關鍵字搜尋
    那麼 回應 422

  場景: MS-08 互斥分頁參數被拒
    當 同時帶入 before 與 around 呼叫訊息列表端點
    那麼 回應 422

  # ---- Error Handling / Boundary ----
  場景: MS-03 排除已刪除訊息
    假設 Bob 送出訊息 "這則稍後會刪"
    並且 Bob 刪除該則訊息
    當 Alice 以關鍵字 "稍後會刪" 搜尋
    那麼 結果為空

  場景: MS-09 萬用字元逸出
    假設 Bob 送出訊息 "折扣 50% 起"
    並且 Bob 另送一則 "完全無關的句子"
    當 Alice 以關鍵字 "50%" 搜尋
    那麼 結果只包含含 "50%" 的那則
    並且 不會因 % 被當萬用字元而誤命中其他訊息

  場景: MS-10 分頁不重不漏
    假設 與 Alice 的對話中有 25 則含關鍵字 "報表" 的訊息
    當 Alice 以 limit=20 搜尋 "報表"
    那麼 第一頁回 20 則並帶 next_before
    並且 以 next_before 取第二頁回剩餘 5 則且與第一頁不重複

  場景: MS-11 around 視窗載入邊界
    假設 與 Alice 的對話中某則訊息為該對話第一則
    當 以 around 指向該則訊息載入視窗
    那麼 回傳含該則與其後鄰居的訊息，且不報錯
