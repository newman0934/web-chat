/**
 * E2E spec: 訊息搜尋 前端 UI 驗收(瀏覽器)
 *
 * 對應 BDD MS-07:搜尋 → 點結果 → 跳轉到對話 → 命中訊息可見且高亮。
 * Task 0 骨架;Task 6 實作轉綠。
 */
import { test } from "@playwright/test";

test.fixme("MS-07 點搜尋結果跳轉並高亮", async () => {});
